Run all tests in this project and collect failures. For each failing test file, spawn a sub-agent to: 1) read the test and source code, 2) diagnose the root cause, 3) fix the source (not the test) unless the test itself is wrong, 4) re-run just that test to verify. Continue looping until all tests pass. After each fix, run the full suite to check for regressions. Commit each logical fix separately with a descriptive message. Do not ask me for input — use test output as your feedback loop.