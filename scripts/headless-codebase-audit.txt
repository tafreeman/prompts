Perform a comprehensive architecture audit of this repository. Create a TODO list with these parallel workstreams, each handled by a sub-agent that writes results to docs/audit/: 1) dependency-analysis.md — map all dependencies, flag outdated/vulnerable ones, 2) code-quality.md — identify dead code, duplicated logic, and complexity hotspots, 3) test-coverage.md — analyze coverage gaps and suggest critical missing tests, 4) api-surface.md — document all public interfaces and inconsistencies, 5) performance.md — flag N+1 queries, unnecessary re-renders, or heavy imports, 6) security.md — check for hardcoded secrets, eval/exec usage, CORS misconfig, path traversal, 7) documentation.md — check for missing docstrings, outdated READMEs, orphaned modules. Each agent must write its file before finishing. After all complete, synthesize a prioritized top-10 action plan in docs/audit/summary.md.