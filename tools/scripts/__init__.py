"""Internal scripts for the prompt library tooling.

These modules are primarily intended to be executed as scripts, but some are
imported by wrappers (e.g., `tools/validate_prompts.py`) and tests.
"""
